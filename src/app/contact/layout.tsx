import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact DANDY",
  description: "Contact DANDY in Karur — bags for school, college, travel and everyday life. Wholesale enquiries welcome.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
